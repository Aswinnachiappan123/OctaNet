import React from 'react';

const PlayerProfile = ({ playerName, playerStats }) => {
  return (
    <div>
      <h2>{playerName}</h2>
      {/* Display player statistics */}
    </div>
  );
}

export default PlayerProfile;